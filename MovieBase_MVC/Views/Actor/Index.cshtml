@model IEnumerable<MovieBase.Models.ActorListItem>

@{
    ViewBag.Title = "Index";
}

<div class="jumbotron" id="homepage">
    <h2>Actor List</h2>
</div>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.ActorId)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.ActorId)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td> </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ActorId }) |
            @Html.ActionLink("Details", "Details", new { id = item.ActorId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ActorId })
        </td>
    </tr>
}

</table>
